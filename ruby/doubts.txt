string.encode
string.force_encoding
string.encoding
grapheme_cluster
each_grapheme
unicode_normalize
unicode_normalized?
unpack


Exception & IO:

begin
   file = open("/unexistant_file")
   if file
      puts "File opened successfully"
   end
rescue
   fname = "existant_file"             # here
   retry
end



begin
    raise 'A psudo exception.'

rescue Exception => e              # passing the exception to the variable [e ]
    puts e.message              # <- here
    puts e.backtrace.inspect    # <- here
                
    puts e.class

ensure
    puts "This will execute Always."

end

----

